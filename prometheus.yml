global:
  scrape_interval: 5s # Busca métricas a cada 5 segundos

scrape_configs:
  - job_name: 'http-server-projeto-korp'
    static_configs:
      # O Prometheus usa o DNS do Docker para achar o container na rede pelo nome do serviço
      - targets: ['http-server-projeto-korp:8080']